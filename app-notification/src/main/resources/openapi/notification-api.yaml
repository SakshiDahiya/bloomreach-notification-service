openapi: 3.0.3
info:
  title: Notification API
  description: API documentation for the notification service
  version: v1
paths:
  /api/notifications:
    post:
      tags:
        - Notifications
      summary: Create a notification
      operationId: createNotification
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateNotificationRequest'
      responses:
        '202':
          description: Notification accepted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateNotificationResponse'
        '400':
          description: Invalid notification request
components:
  schemas:
    CreateNotificationRequest:
      type: object
      description: Request to create a notification
      required:
        - title
        - message
        - severity
        - notificationType
        - audience
      properties:
        title:
          type: string
          minLength: 1
          description: Notification title
          example: Scheduled maintenance
        message:
          type: string
          minLength: 1
          description: Notification message
          example: The system will be unavailable tonight from 11 PM to 12 AM.
        severity:
          $ref: '#/components/schemas/NotificationSeverity'
        notificationType:
          $ref: '#/components/schemas/NotificationType'
        audience:
          $ref: '#/components/schemas/NotificationAudience'
        emailPayload:
          $ref: '#/components/schemas/EmailPayload'
        webappPayload:
          $ref: '#/components/schemas/WebappPayload'
    EmailPayload:
      type: object
      description: Payload used when delivering notifications by email
      required:
        - subject
        - body
      properties:
        subject:
          type: string
          minLength: 1
          description: Email subject
          example: Scheduled maintenance
        body:
          type: string
          minLength: 1
          description: Email body
          example: The system will be unavailable tonight from 11 PM to 12 AM.
    WebappPayload:
      type: object
      description: Payload used when delivering notifications in the web app
      required:
        - title
        - description
        - action
      properties:
        title:
          type: string
          minLength: 1
          description: Web app notification title
          example: Scheduled maintenance
        description:
          type: string
          minLength: 1
          description: Web app notification description
          example: The system will be unavailable tonight from 11 PM to 12 AM.
        action:
          type: string
          minLength: 1
          description: Call to action label or route
          example: View details
    NotificationAudience:
      type: object
      description: Notification target audience
      required:
        - userIds
        - groups
        - labels
      properties:
        userIds:
          type: array
          description: User ids that should receive the notification
          example:
            - user-1
            - user-2
          items:
            type: string
        groups:
          type: array
          description: Groups that should receive the notification
          example:
            - admins
            - support
          items:
            type: string
        labels:
          type: object
          description: Additional audience labels
          additionalProperties:
            type: string
          example:
            region: us
            tier: gold
    NotificationSeverity:
      type: string
      description: Notification severity
      enum:
        - INFO
        - WARNING
        - CRITICAL
      example: INFO
    NotificationType:
      type: string
      description: Notification delivery channel
      enum:
        - EMAIL
        - WEBAPP
      example: EMAIL
    CreateNotificationResponse:
      type: object
      description: Notification creation response
      required:
        - id
      properties:
        id:
          type: string
          format: uuid
